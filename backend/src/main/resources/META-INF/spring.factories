# Registered here rather than as a @Bean so it runs before the application context is
# created — see ProductionConfigurationValidator for why that ordering matters.
org.springframework.boot.env.EnvironmentPostProcessor=\
  dev.lumen.config.ProductionConfigurationValidator
