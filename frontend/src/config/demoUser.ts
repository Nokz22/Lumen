/**
 * Phase 1 has no authentication yet (Phase 2). This is the fixed UUID seeded by
 * V3__seed_demo_user.sql for the dev profile — replaced by the authenticated
 * principal once login exists.
 */
export const DEMO_USER_ID = '11111111-1111-1111-1111-111111111111'
